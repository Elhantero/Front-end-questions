export const selectCategories = (state) => state?.categories?.data;
export const selectCategoriesOrder = (state) => state?.categories?.order;
export const selectCurrentCategoryId = (state) => state?.categories?.currentCategoryId;
export const selectCategoryNameMapToId = (state) => state?.categories?.categoryNameMapToId;
